const {addStr} = require('./utils')
var assert = require('assert');

const targetObj = {"key" : "value", "key2" : {"key3": "value2"}}
, localStr = []

describe('addStr', function () {
it('should return', function () {
      assert.equal([1, 2, 3].indexOf(4), -1);
    });
})
